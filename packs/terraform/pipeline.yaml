pipelines:
  release:
    pipeline:
      agent:
        image: terraform
      stages:
      - name: wenlong
        steps:
        - name: case1
          sh: |
            ls -l
            terraform version
